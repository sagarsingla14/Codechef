#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define mod 1000000007

int main() {
    ll t;
    cin >> t;
    while(t--) {
        ll n;
        cin >> n;
        ll ans = n;
        // vector <pair <ll , ll> > v;
        // ll arr[n] = {0};
        // for(ll i = 0; i < n ; i++) {
        //     cin >> arr[i];
        // }
        // sort(arr , arr + n);
        cout << ans << endl;
    }
    return 0;
}
