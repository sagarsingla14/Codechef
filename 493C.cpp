#include <bits/stdc++.h>
#define mod 998244353
#define ll long long
using namespace std;
int main() {
    ll n ;
    cin >> n;
    ll a[n] = {0};
    for(ll i = 0 ; i < n ; i++) {
        cin >> a[i];
    }
    sort(a , a + n);
    ll m ;
    cin >> m;
    ll b[m] = {0};
    for(ll i = 0 ; i < m ; i++) {
        cin >> b[i];
    }
    sort(b , b + m);
    for(ll i = 0 ; i < n ; i++) {
        
    }
}
