#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main() {
    ll n;
    cin >> n;
    ll arr[n] = {0};
    ll p = 0 , b = 0;
    for(ll i = 0 ; i < n ; i++) {
        cin >> arr[i];
        p += arr[i];
    }
    sort(arr, arr+n) ;

    ll ans = 0;

    for(ll i = 0 ; i < n ; i ++) {
        ll per =
    }

}
