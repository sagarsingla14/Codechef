#include<bits/stdc++.h>
#define ll long long int
using namespace std;
int main() {
    ll t;
    cin >> t;
    while(t--) {
        ll n , w, x , y;
        cin >> n >> w >> x >> y;
        ll ww[n],nn[n],wwc[n],nnc[n];
        for(ll i = 0 ; i < n ; i++) {
            cin>>ww[i];
        }
        for(ll i = 0 ; i < n ; i++) {
            cin>>nn[i];
        }
        ll ans=INT_MIN;
        pair<ll,ll> wu[n],nu[n];
    }
}
